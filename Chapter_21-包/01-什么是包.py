"""
    1.包的定义：
        包将有联系的模块组织在一起，即放到同一个文件夹下，并且在这个文件夹创建一个名字为__init__.py的文件，这个文件夹就是包.
    2.包的制作步骤：
        [NEW]-[Python Package]-输入包名-[OK]-新建功能模块(.py文件)
        注意：新建包后，包内部会自动创建__init__.py文件，这个文件控制着包的导入行为
"""